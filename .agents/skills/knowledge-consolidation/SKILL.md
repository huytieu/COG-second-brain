---
name: knowledge-consolidation
description: Build frameworks from scattered insights across all braindumps and notes
---

Read `.claude/skills/knowledge-consolidation/SKILL.md` and execute it exactly as written — that file is the authoritative
playbook. Then follow `.agents/rules/cog.md`.

Antigravity substitution: where the playbook delegates to a `.claude/agents/<name>`
worker, invoke `.agents/agents/<name>.md` via `invoke_subagent` instead.
